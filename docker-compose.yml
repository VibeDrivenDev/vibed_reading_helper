services:
  reading-helper:
    build: .
    ports:
      - "8080:80"
    env_file:
      - .env
    environment:
      AUDIO_ENABLED: ${AUDIO_ENABLED:-false}
      AUDIO_RATE: ${AUDIO_RATE:-0.85}
    restart: unless-stopped
